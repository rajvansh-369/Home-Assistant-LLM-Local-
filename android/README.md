# Aster (Android)

Expo / React Native client. See `CLAUDE.md` for commands and layout, and
`docs/aster-first-run-plan.md` for the build plan.

```bash
cp .env.example .env   # fill in ANDROID_PACKAGE etc.
npm install
npm run android        # builds and installs the dev build
```
